import { View, StyleSheet } from 'react-native';
import { MatchScreenProps } from '../../views/match/MatchesList';

interface MatchScreenBoxProps {
	data: MatchScreenProps;
}

export default function MatchScreenBox(data: MatchScreenBoxProps) {
	return <View></View>;
}

const styles = StyleSheet.create({
	container: {},
});
